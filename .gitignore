# Secrets — never commit these.
.env
.env.local
.env*.local

# Python
.venv/
__pycache__/
*.pyc

# Pipeline working files. These are big (videos, audio, every sampled frame) and
# fully reproducible from a re-run. What the website actually serves is exported
# into public/ and IS committed.
output/

# Tooling
node_modules/
.vercel/
.DS_Store
*.log
